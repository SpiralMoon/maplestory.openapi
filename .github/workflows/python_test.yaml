name: Test Python

on:
  workflow_dispatch:
  push:
    paths:
      - python/**

jobs:
  test_python:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: './python'

    env:
      API_KEY_KMS: ${{ secrets.API_KEY_KMS }}
      API_KEY_MSEA: ${{ secrets.API_KEY_MSEA }}
      API_KEY_TMS: ${{ secrets.API_KEY_TMS }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          echo "Installing dependencies"
          pip3 install wheel twine
          pip3 install -r requirements.txt

      - name: Build package
        run: |
          echo "Build Python project"
          python3 setup.py sdist bdist_wheel

      - name: Test
        run: |
          echo "API_KEY_KMS=${API_KEY_KMS}" >> $GITHUB_ENV
          echo "API_KEY_MSEA=${API_KEY_MSEA}" >> $GITHUB_ENV
          echo "API_KEY_TMS=${API_KEY_TMS}" >> $GITHUB_ENV
          echo "Testing"
          pytest
